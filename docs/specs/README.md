# Feature Specs

Put approved feature specs in this directory when they do not belong closer to a specific backend service or mobile screen.

Use [the feature spec template](../templates/feature-spec.md). Each spec should define behavior, contracts, data shape, ownership, acceptance criteria, and verification before implementation starts.
