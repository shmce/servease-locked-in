
  # (claire copy) Mobile App Authentication Flow (Copy) (Copy)

  This is a code bundle for (claire copy) Mobile App Authentication Flow (Copy) (Copy). The original project is available at https://www.figma.com/design/GSn7FrTQ4PWBqcbAwx0hH1/-claire-copy--Mobile-App-Authentication-Flow--Copy---Copy-.

  ## Running the code

  Run `npm i` to install the dependencies.

  Run `npm run dev` to start the development server.
  