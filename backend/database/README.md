# Database Migrations

Place backend database migrations here once service schemas are approved.

Each migration must name the owning service and must not introduce cross-service database access or cross-schema foreign keys.
